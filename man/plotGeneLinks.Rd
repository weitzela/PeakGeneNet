% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualization.R
\name{plotGeneLinks}
\alias{plotGeneLinks}
\title{Plot peak-to-gene links for a single gene}
\usage{
plotGeneLinks(
  ensembl_to_plot,
  pk2g_res,
  peak_stats,
  gene_info,
  contrast_labels = c("Up", "Down"),
  contrast_colors = c("#E78AC3", "#A6D854"),
  link_dir_colors = c("#B2182B", "#2166AC"),
  modality_colors = NULL,
  peak_color = "black",
  peak_alpha = 0.9,
  peak_height_npc = 0.025,
  peak_width_npc = 0.01,
  bkg_lighten = 0.8,
  feature_track = NULL,
  feature_track_height_npc = 0.05,
  feature_track_color = "gray25",
  include_other_gene_bodies = TRUE,
  scale_bar_width_bp = 2e+05
)
}
\arguments{
\item{ensembl_to_plot}{Character. Ensembl gene ID to plot. Must be in
\code{names(gene_info)}.}

\item{pk2g_res}{Data frame, typically the output of \code{\link[=processCorrelations]{processCorrelations()}}.
Must contain columns \code{ensembl_gene_id}, \code{regulatory_element},
\code{target_id}, \code{P}, and \code{region2gene_dir}.}

\item{peak_stats}{\code{GRanges} keyed by \code{regulatory_element} (i.e.
\code{names(peak_stats)} matches values in \code{pk2g_res$regulatory_element},
which follow the \code{unique_id} convention from \code{\link[=createPeakGr]{createPeakGr()}}:
\code{"chr:start-end_modality"}). Must have \code{log2FoldChange} and \code{pvalue}
metadata columns, e.g. attached from a DESeq2 analysis on the same peaks.}

\item{gene_info}{\code{GRanges} keyed by \code{ensembl_gene_id}, with one entry per
gene whose \code{start}/\code{end} define the gene body and \code{strand} must be \code{"+"}
or \code{"-"}. The TSS is derived as the strand-aware 5' end of the gene
body (\code{start} for \code{"+"}, \code{end} for \code{"-"}). Typically obtained by
subsetting the \code{gene_gr} output of \code{\link[=createPeak2GeneObjects]{createPeak2GeneObjects()}} to
\code{annotation == "GeneBody"}.}

\item{contrast_labels}{Length-2 character vector. Labels for the two
contrast halves (positive vs negative peak log2FC). Default:
\code{c("Up", "Down")}.}

\item{contrast_colors}{Length-2 character vector. Background tint colors
for the two contrast halves. Distinct from \code{link_dir_colors} because
the two encode different things — peak direction vs link direction.
Default: \code{c("#E78AC3", "#A6D854")}.}

\item{link_dir_colors}{Length-2 character vector. Colors for positive
vs negative peak-to-gene correlation links (the bezier curves).
Default: \code{c("#B2182B", "#2166AC")}.}

\item{modality_colors}{Optional named character vector mapping modality
to fill color, e.g. \code{c(ATACSeq = "#abc", H3K27ac = "#def")}. When
non-NULL, peaks are colored by modality and given a modality-specific
vertical offset so overlapping peaks remain visually distinguishable.
When \code{NULL} (default), all peaks are filled with \code{peak_color}.}

\item{peak_color}{Single fill color for peak triangles when
\code{modality_colors = NULL}. Default: \code{"black"}.}

\item{peak_alpha}{Alpha for peak triangle fill. Default: \code{0.9}.}

\item{peak_height_npc}{Peak triangle apex height in NPC units. Default:
\code{0.025}.}

\item{peak_width_npc}{Peak triangle width as a fraction of the locus
width. Default: \code{0.01}.}

\item{bkg_lighten}{Lightening factor passed to \code{\link[colorspace:lighten]{colorspace::lighten()}}
for the contrast background tints. Default: \code{0.8}.}

\item{feature_track}{Optional \code{GRanges} of genomic features to mark on
the midline (e.g. SNPs, TF binding sites). Features overlapping the
plotted locus are drawn as short vertical ticks at their start
coordinates. Pre-filter the input GRanges to control which features
appear. Default: \code{NULL}.}

\item{feature_track_height_npc}{Half-height of feature ticks in NPC
units. Default: \code{0.05}.}

\item{feature_track_color}{Tick color for the feature track. Default:
\code{"gray25"}.}

\item{include_other_gene_bodies}{If \code{TRUE} (default), other gene bodies
overlapping the plotted locus are drawn as small rectangles on the
midline.}

\item{scale_bar_width_bp}{Width of the scale bar in bp. Default: \code{2e5}.}
}
\value{
A \code{ggplot} object with a \code{"ylim"} attribute giving the symmetric
y-limits used for \code{coord_cartesian}.
}
\description{
Visualises peak-to-gene links from PeakGeneNet output for a single gene,
drawing each link as a bezier curve from the peak to the gene's TSS. The
plot is split horizontally around the gene-body midline: peaks with
positive log2FC sit above (\code{contrast_labels[1]}), peaks with negative
log2FC sit below (\code{contrast_labels[2]}). Bezier apex height encodes the
peak-to-gene correlation p-value, color encodes correlation direction
(\code{link_dir_colors}), and line thickness encodes the peak's own DESeq2
p-value.
}
\details{
Optional layers:
\itemize{
\item \code{feature_track}: short vertical ticks across the midline marking
arbitrary genomic features (SNPs, TF binding sites, etc.) overlapping
the locus. Pre-filter the input GRanges to control which features appear.
\item \code{modality_colors}: per-modality fill coloring of peak triangles, with
per-modality height offsets so overlapping peaks remain distinguishable.
\item \code{include_other_gene_bodies}: rectangles for other gene bodies
overlapping the locus.
}
}
