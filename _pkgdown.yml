url: https://weitzela.github.io/PeakGeneNet/

development:
  mode: auto

template:
  bootstrap: 5
  # package: tidytemplate

navbar:
  structure:
    left: [setup, reference, articles]
    right: [search, github]
